{
  "name": "linktree-api",
  "version": "1.0.0",
  "description": "Social media link aggregator API for businesses",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init-db": "node -e \"require('pg').Client.prototype.connect = function() { console.log('This is for local setup only. Configure DATABASE_URL in .env'); process.exit(1); }; const { execSync } = require('child_process'); execSync('psql -U postgres -f init-db.sql', { stdio: 'inherit' });\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "linktree",
    "social-media",
    "business",
    "api",
    "express"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.10.0",
    "jsonwebtoken": "^9.1.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express-validator": "^7.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
